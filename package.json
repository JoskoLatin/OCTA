{
  "name": "octa",
  "version": "1.0.0",
  "description": "OCTA — 8-voice groovebox",
  "private": true,
  "scripts": {
    "serve": "serve -l 8080",
    "sync-www": "node tools/sync-www.mjs",
    "apk": "npm run sync-www && cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  }
}
